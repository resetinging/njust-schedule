<!-- 主页面: swiper 承载 5 个 Tab 视图(课表/考试/评教/成绩/我的) -->
<view class="main-page">

  <!-- 顶部公告条: 有新公告(updated 未读)时展示, 查看后隐藏 -->
  <view class="ann-bar" wx:if="{{ann.visible}}" bindtap="onAnnTap">
    <text class="ann-icon">📢</text>
    <text class="ann-text">{{ann.text}}</text>
    <text class="ann-close" catchtap="onAnnClose">✕</text>
  </view>

  <swiper class="main-swiper"
          style="height: {{swiperHeight}}px"
          current="{{current}}"
          bindchange="onSwiperChange"
          bindtransition="onSwiperTransition"
          duration="260">
    <swiper-item wx:for="{{tabs}}" wx:key="index">
      <schedule-view wx:if="{{index === 0}}" id="tabview0" />
      <exams-view wx:elif="{{index === 1}}" id="tabview1" />
      <eval-view wx:elif="{{index === 2}}" id="tabview2" />
      <grades-view wx:elif="{{index === 3}}" id="tabview3" />
      <settings-view wx:elif="{{index === 4}}" id="tabview4" />
    </swiper-item>
  </swiper>

  <!-- 自定义 tabBar(手动引用: main 不在 tabBar list 中, 需自渲染) -->
  <custom-tab-bar id="tabbar" />
</view>
