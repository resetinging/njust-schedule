<!-- 空教室查询页: 筛选卡片 + 教室列表 -->
<view class="container">

  <!-- ============ 已登录: 筛选 + 结果 ============ -->
  <block wx:if="{{loggedIn}}">

    <!-- 筛选卡片 -->
    <view class="card filter-card">
      <picker mode="selector" range="{{campusList}}" value="{{campusIndex}}" bindchange="onCampusChange">
        <view class="filter-row">
          <text class="filter-label">校区</text>
          <text class="filter-value">{{campusList[campusIndex]}}</text>
          <text class="arrow">›</text>
        </view>
      </picker>
      <picker mode="selector" range="{{weekdayList}}" value="{{weekdayIndex}}" bindchange="onWeekdayChange">
        <view class="filter-row">
          <text class="filter-label">星期</text>
          <text class="filter-value">{{weekdayList[weekdayIndex]}}</text>
          <text class="arrow">›</text>
        </view>
      </picker>
      <picker mode="selector" range="{{weekList}}" value="{{weekIndex}}" bindchange="onWeekChange">
        <view class="filter-row">
          <text class="filter-label">周次</text>
          <text class="filter-value">{{weekList[weekIndex]}}</text>
          <text class="arrow">›</text>
        </view>
      </picker>
      <picker mode="selector" range="{{slotList}}" range-key="label" value="{{slotIndex}}" bindchange="onSlotChange">
        <view class="filter-row">
          <text class="filter-label">时段</text>
          <text class="filter-value">{{slotList[slotIndex].label}}</text>
          <text class="arrow">›</text>
        </view>
      </picker>
      <picker mode="selector" range="{{buildingList}}" value="{{buildingIndex}}" bindchange="onBuildingChange">
        <view class="filter-row">
          <text class="filter-label">教学楼</text>
          <text class="filter-value">{{buildingList[buildingIndex]}}</text>
          <text class="arrow">›</text>
        </view>
      </picker>
    </view>

    <!-- 结果摘要 -->
    <view class="result-head" wx:if="{{result}}">
      <text class="result-summary">{{result.summary}}</text>
      <text class="result-count">{{result.count}} 间空闲</text>
    </view>

    <!-- 教室列表(点击复制) -->
    <view class="room-wrap" wx:if="{{rooms.length}}">
      <view class="room-chip" wx:for="{{rooms}}" wx:key="*this"
            data-room="{{item}}" bindtap="onCopyRoom">{{item}}</view>
    </view>

    <!-- 无空闲 -->
    <empty-state wx:elif="{{searched && !loading && !errorMsg}}" icon="🏫" title="该时段没有空闲教室"
                 desc="试试换个时段、周次或校区" />

    <!-- 查询中 / 错误 -->
    <view class="state-tip" wx:if="{{loading}}">⏳ 正在查询空闲教室…</view>
    <view class="err-tip" wx:elif="{{errorMsg}}">{{errorMsg}}</view>
  </block>

  <!-- ============ 未登录 ============ -->
  <view wx:else class="login-wrap">
    <empty-state icon="🔐" title="需要先登录" desc="登录教务后即可查询空闲教室"
                 action-text="去登录" bind:action="goLogin" />
  </view>

</view>
